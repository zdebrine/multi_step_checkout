{
  "name": "msc",
  "version": "1.0.0",
  "description": "Package file for MSC app",
  "main": "index.js",
  "scripts": {
    "test": "react-scripts test",
    "start:server": "nodemon server.js",
    "start:react": "cd frontend && npm start",
    "seed": "mysql -u root -p < schema.sql",
    "build": "cd frontend && npm run-script build && cd ..",
    "install-all": "npm install && cd frontend && npm install && cd .."
  },
  "dependencies": {
    "axios": "^0.19.2",
    "babel-core": "^6.26.3",
    "babel-loader": "^8.1.0",
    "babel-preset-env": "^1.7.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "author": "Zak DeBrine",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-react": "^6.24.1",
    "html-webpack-plugin": "^4.4.1"
  }
}
